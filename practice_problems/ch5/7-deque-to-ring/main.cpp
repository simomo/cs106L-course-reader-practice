#include <deque>

using namespace std;

struct Ring {
    deque<int> _ring;

    // copy constructor & Ring(deque<int> ) constructor

    // Read & Write

    // CursorClockwise & CursorCounterClockwise
}